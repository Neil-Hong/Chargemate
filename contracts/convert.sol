// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

import "@chainlink/contracts/src/v0.8/interfaces/AggregatorV3Interface.sol";

contract ChinalinkUSDTConverter {
    AggregatorV3Interface internal priceFeed;

    constructor(address _priceFeedAddress) {
        priceFeed = AggregatorV3Interface(_priceFeedAddress);
    }

    function getLatestPrice() public view returns (int) {
        (
            uint80 roundID, 
            int price,
            uint startedAt,
            uint timeStamp,
            uint80 answeredInRound
        ) = priceFeed.latestRoundData();
        return price;
    }

    function convertToUSDT(uint cryptoAmount) public view returns (uint) {
        int cryptoPrice = getLatestPrice();
        // Conversion logic: 1 unit of cryptocurrency * price in USDT
        return uint(cryptoPrice) * cryptoAmount;
    }
}